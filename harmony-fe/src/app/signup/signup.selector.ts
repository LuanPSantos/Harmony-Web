// import { createFeatureSelector, createSelector } from '@ngrx/store';
// import AppState, { SignupFeatureState } from '../app.state';
// import { signupFeatureKey } from './signup.reducer';

// export const selectFeature = createFeatureSelector<AppState, SignupFeatureState>(signupFeatureKey);

// export const selectCredentialId = createSelector(
//     selectFeature,
//     (state: SignupFeatureState) => {
//         return state.credential.id
//     }
// )



